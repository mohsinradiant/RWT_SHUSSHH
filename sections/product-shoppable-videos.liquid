<h2 class="section-heading">{{ section.settings.section_heading }}</h2>
<div class="page-width product-shoppable-videos">
  {% for testimonial in product.metafields.content.product_shoppable_videos.value %}
    <div class="product-shoppable-video-block">
      <div class="product-shoppable-video-container">
        <img
          src="{{ testimonial.placeholder | img_url: 'medium' }}"
          class="product-shoppable-video-image"
        >
        <div class="product-shoppable-product-shoppable-video-container">
          {{ testimonial.video.value | video_tag: autoplay: false, loop: true, muted: false, controls: true }}
        </div>
      </div>

      {% assign product = testimonial.product.value %}
      {% if product %}
        <div class="product-card">
          <a href="{{ product.url }}" class="product-link">
            <img
              src="{{ product.featured_image | img_url: 'medium' }}"
              alt="{{ product.title }}"
              class="product-image"
              height="auto"
              width="auto"
            >
            <div class="product-info">
              <h3 class="product-title">{{ product.title }}</h3>
              <p class="product-prices">
                {% if product.compare_at_price > product.price %}
                  <span class="sale-price">{{ product.price | money }}</span>
                  <span class="regular-price">{{ product.compare_at_price | money }}</span>
                {% else %}
                  <span class="regular-price">{{ product.price | money }}</span>
                {% endif %}
              </p>
            </div>
          </a>
        </div>
      {% endif %}
    </div>
  {% endfor %}
</div>
<style>
  .product-shoppable-product-shoppable-video-container {
    background-color: #faf3ff;
  }
  .product-shoppable-videos {
    display: flex;
    gap: 10px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    scroll-behavior: smooth;
    scrollbar-width: none;
  }

  .product-shoppable-videos::-webkit-scrollbar {
    display: none;
  }

  .product-shoppable-video-block {
    display: flex;
    flex-direction: column;
    align-items: center;
    border: 1px solid #ddd;
    padding: 10px;
    border-radius: 10px;
    scroll-snap-align: start;
    min-width: 270px;
  }
  .product-shoppable-video-container img {
    width: 100%;
    display: flex;
    height: auto;
  }
  .product-shoppable-video-container {
    width: 100%;
    aspect-ratio: 147 / 240;
    overflow: hidden;
    border-radius: 10px;
    margin-bottom: 15px;
    height: auto;
  }
  .product-shoppable-video-container video {
    width: 100%;
    height: 100%;
    margin-bottom: 15px;
    aspect-ratio: 147 / 240;
    border-radius: 10px;
  }

  .product-card {
    display: flex;
  }

  .product-link {
    text-decoration: none;
    display: flex;
    justify-content: start;
    gap: 10px;
    align-items: center;
  }

  .product-image {
    width: 25%;
    border-radius: 50%;
  }

  .product-title {
    font-size: 2rem;
    font-weight: 500;
    margin: 0;
    font-family: 'Kanit', sans-serif;
  }

  .product-prices {
    font-size: 1.3rem;
    letter-spacing: 0px;
    margin: 0;
    font-family: 'Kanit', sans-serif;
  }

  .sale-price {
    color: #000000;
    font-weight: 500;
    margin-right: 5px;
  }

  .regular-price {
    text-decoration: line-through;
    color: #00000070;
  }
  @media (max-width: 500px) {
    .product-shoppable-video-block {
      min-width: 220px;
    }
    .product-shoppable-videos {
      margin-left: 10px;
    }
  }
</style>
<script>
  document.addEventListener('DOMContentLoaded', function () {
    const shoppableVideoBlocks = document.querySelectorAll('.product-shoppable-video-container');

    shoppableVideoBlocks.forEach((block) => {
      const image = block.querySelector('.product-shoppable-video-image');
      const shoppableVideoContainer = block.querySelector('.product-shoppable-product-shoppable-video-container');
      const video = shoppableVideoContainer.querySelector('video');

      // Hide the video container initially
      shoppableVideoContainer.style.display = 'none';

      // Add click event to the image
      image.addEventListener('click', () => {
        image.style.display = 'none'; // Hide the image
        shoppableVideoContainer.style.display = 'flex'; // Show the video container
        video.play(); // Play the video
      });

      // Add pause event to the video
      video.addEventListener('pause', () => {
        shoppableVideoContainer.style.display = 'none'; // Hide the video container
        image.style.display = 'flex'; // Show the image
      });
    });
  });
</script>
{% schema %}
{
  "name": "Product Shoppable Videos",
  "settings": [
    {
      "type": "text",
      "id": "section_heading",
      "label": "Section Heading",
      "default": "Discover What’s Making Everyone Go Wild!"
    }
  ],
  "presets": [
    {
      "name": "Product Shoppable Videos"
    }
  ]
}
{% endschema %}
